#!/usr/bin/env sh
set -e

DIRNAME=$(cd "$(dirname "$0")" && pwd)
GRADLE_WRAPPER="$DIRNAME/gradle/wrapper/gradle-wrapper.jar"
if [ ! -f "$GRADLE_WRAPPER" ]; then
  echo "Warning: gradle-wrapper.jar not found. CI systems with system Gradle should still work."
fi

exec "$DIRNAME/gradlew" "$@"
